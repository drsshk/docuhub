# DocuHub Environment Configuration Template
# Copy this file to .env and update with your actual values

# ===========================================
# CRITICAL SETTINGS (Required for startup)
# ===========================================

# Django Secret Key - Generate a new one for production
# You can generate one at: https://djecrety.ir/
SECRET_KEY=your_secret_key_here_make_it_long_and_random

# Debug mode - Set to False in production
DEBUG=True

# ===========================================
# DATABASE CONFIGURATION
# ===========================================

# Option 1: Use DATABASE_URL (recommended for production)
# DATABASE_URL=mysql://username:password@localhost:3306/database_name

# Option 2: Individual database settings (used if DATABASE_URL not set)
DB_NAME=docuhub_db
DB_USER=your_db_username
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=3306

# ===========================================
# WEB SERVER CONFIGURATION
# ===========================================

# Allowed hosts - comma separated list of domains/IPs
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# CORS allowed origins - comma separated list of frontend URLs
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:8000,http://127.0.0.1:8000

# Frontend URL for email links and redirects
FRONTEND_URL=http://localhost:8000

# HTTPS Configuration (set to True in production with SSL)
USE_HTTPS=False

# ===========================================
# EMAIL CONFIGURATION (Brevo)
# ===========================================

# Brevo API Configuration
BREVO_API_KEY=your_brevo_api_key_here
BREVO_SMTP_KEY=your_brevo_smtp_password_here

# Email sender configuration
DEFAULT_FROM_EMAIL=info@docuhub.rujilabs.com
BREVO_SENDER_NAME=DocuHub System

# Email branding (optional)
EMAIL_COMPANY_NAME=DocuHub
EMAIL_LOGO_URL=https://your-domain.com/logo.png
EMAIL_WELCOME_MESSAGE=Your account has been created. Please click the link below to set your password and activate your account.

# Alternative email configuration (if not using Brevo)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your_email@gmail.com
# EMAIL_HOST_PASSWORD=your_email_app_password

# ===========================================
# EMAIL TEMPLATE IDs (Brevo Templates)
# ===========================================

# Brevo email template IDs - update with your actual template IDs
EMAIL_TEMPLATE_PROJECT_SUBMITTED=1
EMAIL_TEMPLATE_PROJECT_APPROVED=2
EMAIL_TEMPLATE_PROJECT_REJECTED=3
EMAIL_TEMPLATE_PROJECT_REVISE_RESUBMIT=4
EMAIL_TEMPLATE_PROJECT_OBSOLETE=5
EMAIL_TEMPLATE_ADMIN_NEW_SUBMISSION=6
EMAIL_TEMPLATE_ADMIN_RESUBMISSION=7

# ===========================================
# REDIS CONFIGURATION (Optional)
# ===========================================

# Redis URL for caching and Celery task queue
REDIS_URL=redis://localhost:6379/0

# ===========================================
# MONITORING & ERROR TRACKING (Optional)
# ===========================================

# Sentry DSN for error tracking (optional)
# SENTRY_DSN=https://your_sentry_dsn_here

# ===========================================
# DEVELOPMENT NOTES
# ===========================================

# 1. Copy this file to .env in the same directory
# 2. Update all values with your actual configuration
# 3. Never commit your actual .env file to version control
# 4. Generate a new SECRET_KEY for production environments
# 5. Set DEBUG=False in production
# 6. Configure proper SSL certificates if using HTTPS=True

# ===========================================
# PRODUCTION SECURITY CHECKLIST
# ===========================================

# ✅ Set DEBUG=False
# ✅ Generate new SECRET_KEY (never reuse development keys)
# ✅ Configure proper ALLOWED_HOSTS
# ✅ Set up SSL certificates if using HTTPS=True
# ✅ Configure secure database credentials
# ✅ Set up email service (Brevo recommended)
# ✅ Configure Redis for production caching
# ✅ Set up Sentry for error monitoring
# ✅ Review and update CORS_ALLOWED_ORIGINS

# ===========================================
# QUICK START FOR DEVELOPMENT
# ===========================================

# 1. Install MySQL and create database:
#    CREATE DATABASE docuhub_db;
#    CREATE USER 'docuhub_user'@'localhost' IDENTIFIED BY 'your_password';
#    GRANT ALL PRIVILEGES ON docuhub_db.* TO 'docuhub_user'@'localhost';

# 2. Install Redis (optional but recommended):
#    sudo apt install redis-server  # Ubuntu/Debian
#    brew install redis             # macOS

# 3. Update this file with your database credentials
# 4. Copy to .env and run: python manage.py migrate
# 5. Create superuser: python manage.py createsuperuser
# 6. Start development server: python manage.py runserver